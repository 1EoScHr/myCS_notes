**操作系统：MIT 6.S081 Operating System Engineering**  

***

# Lec1 Intro & Example

## OS Purposes 操作系统的目的

+ 实现对**硬件**的**抽象**
+ 不同程序中**复用硬件**
+ 不同活动**不互相干涉**
+ 能够实现某些**资源共享**
+ 拥有**安全性**
+ **性能优异**
+ **多用途**

## 操作系统的构成

比较经典、公认的操作系统结构，操作系统课程格外关心**Kernel**以及其与用户空间的接口：  

1. **用户空间 User Space**（最顶层）：运行各种应用程序，如Vi、CC、SH等程序
2. **接口 Kernel API**：用户空间与内核的交互层，调用类似函数，但特殊在是直接进入内核而不是另一个函数，这特别在内核是一个拥有特权、直接访问硬件等的，始终驻留
2. **内核 Kernel**（中间层）：计算机资源的守护者，用于管理数据、进程、硬件等，开机第一个运行，拥有诸多内置服务，如文件系统、进程管理、内存分配、访存控制
3. **硬件 Hardware**（最底层）：CPU、RAM、Disk、Net...  

## Why Hard/Interesting

~~向大佬学习~~  

*There will be no exams or quizzes this year, And so what, that means is that most of the, you know 90% of the grade is being driven by the lab, so you should spend a lot of time in the labs, make sure that you start early and have enough time to complete them and work out bugs in order to get full credit.*  

## 系统调用实例

这里讲的、演示的应该都是在xv6上的。  

**系统调用**与**函数调用**方法类似，但也有区别，系统调用是操作系统提供服务的接口。  

### 系统调用不同于C语言函数

像read、write、open这几个**系统调用函数**，**不是**一个C函数，**而是**RISC-V的汇编命令集合而成的（课程的xv6系统基于RISC-V），涉及一个**ecall**命令，其将控制权转移到内核中，内核调用进程内存、寄存器来找出参数。  

### 接口举例/用法

譬如演示中xv6命令行的**copy**命令，调用了**read**、**write**两个系统接口。  
（但这里的代码没看明白：用`while(1)`这样的死循环，退出路径只有**read**的返回值n不大于0的时候，又说read返回值是读取的字节数，那么write后，到第二轮循环时运行到read，其又该以什么机制如何返回一个不大于0的数来退出？我的猜测是：read读取顺利的情况下返回读取的字节数，然后对读取的文件做一个标记，可能是“已经读过了”，下一次读不到，也可能是改变可读属性，当第二个循环时，读到0或者读不到报异常，返回0或负数，直接退出）  

+ `read(文件描述符, 内存地址, 最大字节数)`，从文件描述符对应的文件读取最大字节数的数据，写到内存地址处；返回值是读取到的字节数  
+ `write(文件描述符, 内存地址, byte)`，从内存地址读取byte个字节，写到文件描述符对应的文件中，以字节流的形式，并不指定具体数据类型  
+ `open(文件名, O_flags)`，以flags标志位指定的方法（创建/打开/读取/写入）打开一个名为文件名的文件，更深层的是将对应文件与文件标识符联系起来；返回打开文件对应的文件标识符  
+ `fork()`，执行fork命令的必然是**当前的进程**，fork命令会创建一个当前进程的**副本**，包含进程内所有指令、数据、堆栈等（理论上是内容完全一样，只是物理地址不同的两块；现代操作系统使用写时复制技术优化性能，后面会讲）。这样我们就有两个内存完全相同的进程，一个是原进程，另一个是原进程的副本，前者为**父进程**，后者为**子进程**。返回值则根据进程不同而区别，父进程返回值是子进程的pid，而子进程返回值是0，这可以用来区分父子进程，从而预设好分支。  
（这里看网课的翻译没看懂，其实就是fork后程序/进程依然要继续运行，那么父子进程都会同时继续运行，可以通过返回值区分；翻译导致糊涂认为返回两个值......）  
演示中还有一个地方有趣，就是不像GPT展示的父子进程输出是分开的，而是两个同时运行，各自输出各自的，几乎成了乱码。原因是QEMU模拟多核微处理器，GPT的回答可能是没考虑这个或者有其他调度方法。  
+ `exec(程序路径, *[参数]， (char*)0)`，功能是用命令来取代当前的进程，可以选择传递参数。exec会保留当前进程的文件描述符表。其不会返回值，除非找不到路径等招致的执行失败。一般把参数和空指针写在一起：`char* argv[] = {"xx", "yy", ..., 0}`，这样就能规范的用`exec(path, argv)`了  
（这里的例子又没看懂：`exec("echo", "echo", "this", "is", "echo", (char*)0)`，最后的输出却是“this is echo”，这里就有几个要点，第一个参数是执行程序的路径，然后第二个参数是告诉程序自己的名字是什么，原则上就是程序名，但其实无所谓，然后最后一个参数必须要是(char\*)0，也就是NULL来结束，否则会一直往下读，因此这里实际等效的是`echo this is echo`）  
+ `wait()`，是与fork()配套的系统调用，用来让父进程阻塞，直至子进程结束，能够控制执行顺序、清理结束的子进程资源。其返回结束的子进程的pid，能够方便在多个子进程时明确是哪一个退出了。还有一个可选参数，类型是int指针（int\* status），会保存子进程的返回值。  
值得注意的是，当有多个子进程时，wait会在任意一个子进程结束后停止阻塞，要想完全回收，有几个就回收几下；而有一个waitpid()，可以指明等待哪一个子进程；父进程与孙进程没有直接联系；uinx没有子进程wait父进程的系统调用。  
+ `close(文件标识符)`，关闭文件标识符对应的文件。常常与**fork()**、**open()**结合，来实现主进程不变、子进程输入输出重定向。  

### 文件描述符

通常**文件描述符**为0是代表进程的输入，为1是进程的输出；也可以用**open**接口来创建一个新的文件标识符与实际文件的连接（linux的理念是“***一切皆文件***”，比如外设是文件，所以命令行的信息也算是文件），实际上是沿着0、1往下的其他小数字。后面在**输出重定向**中，实际上是关闭了1，然后open一个文件，会自动**分配最小的空闲文件标识符**，新文件就变成了1，则向其输出而实现输出重定向。  
更具体的，**文件描述符**实际上是对需要用的文件的一个**索引**（描述符->具体文件）。内核维护着的每一个进程（运行的程序）都有一个table，记录进程用到的文件描述符，如此就能将**文件描述符空间**与**真实文件**对应起来。由于每一个进程都单独维护一张表，所以不同进程中**同一个**文件描述符**不一定**对应同一个文件，仅仅是对进程所需文件的一个索引。  

### shell

**shell**称为**命令行接口**，有相对图形化的界面，在unix系统中广泛使用来进行各种操作。  

之前看的一个科普：shell的意思是外壳，而kernel是内核，两者存在交汇的地方，很有趣且形象，是用户层里离内核最近的地方。  

通常在shell中执行命令，譬如`ls`，意思是要求shell运行名为“**ls**”的程序。  
但**真实发生**的事情是在文件系统中有一个名为**ls**的文件，其内部包含了能够实现其功能的机器指令，shell于是便执行了**ls**中的机器指令。（还是“一切皆文件”思想的体现）  

shell执行命令，其实是**fork+exec**的组合，主进程维持shell本身，子进程替换为要执行的命令。是很常见的unix方法。  

### 进程树 Process Tree

在了解进程的`fork()`相关内容时，发现**进程树**的概念，在类unix系统中，所有的进程都能追溯到init进程，在linux中可以通过`pstree`命令查看，实践一番，果然，追溯到了“**systemd**”进程，十分神奇。  

当出现某种特殊情况导致一个进程变为孤儿进程时，操作系统会把孤儿进程挂到init(systemd)进程下。  

***

# Lec3 OS Organization

据评论区称，Lec2是一个介绍基本操作的ppt，无妨。  

**topic：isolation隔离，kernel/user mode内核/用户模式，system call系统调用**  

## isolation 隔离性

OS基于硬件的抽象，提供许多应用的服务；但是诸多应用之一出现bug，并不会导致其他应用程序操作系统崩溃，这就是我们希望的**应用程序与操作系统之间有强隔离**。  

### 隔离性意义：从一个strawman设计

（英文语境中，strawman是一个预设的立场，一般要在接下来被打倒）  

考虑隔离性的意义，按照“**如果没有操作系统会怎样**”的思维方式来看：  

1. 如果没有操作系统，各个程序访问到的**不是抽象后的硬件，而是真实的硬件**，而即使一台计算机即使没有操作系统，也应该能够**同时运行**多个应用程序。所以一种方案是**正在运行的程序应一段时间后主动的放弃占用的硬件**，来实现同时运行。但是程序本身如果进入死循环卡死，就没有任何方式能令其退出。**bug：缺少enforced multilplexing（强制复用）**
2. 两个需要同时运行的程序，数据直接写在物理内存上，可能会导致**相互覆写**。**bug：缺少memory isolation（内存隔离）**

由此可见，操作系统的重点在于**在复用的同时，有强隔离性**，而没有操作系统、任由程序访问底层硬件，很难实现隔离。  

### Unix interface 实现

Unix类系统可以解决隔离性的问题，方法在于**其使用的接口抽象了底层硬件资源**。  

+ **process**，是对**CPU资源**的抽象：使内核主动的在进程之间复用，使操作系统可以**复用一个或多个CPU**到**多个应用程序**。  
+ **exec**是对**memory资源**的抽象：一个正在运行的程序本质是**内存里的一堆数据**，当要让当前进程执行一个新的程序时，本质上是要把内存里的相关数据都更换。没有操作系统与exec接口时，就需要手动把这些代码段数据段堆栈手动分配到物理内存；而有这个接口时就忽略了memory中发生的这些。  
+ **files**是对**disk block磁盘块**的抽象：应用程序只能通过文件系统来访问存储系统，而不允许直接去底层读取磁盘块。程序只用访问文件抽象的接口，而底层的写入写出磁盘、用户私有文件隔离等都被省略了。  

再次重复，由此可见，操作系统通过抽象硬件，实现**资源复用**与**强隔离**。  

## OS should be defensive 防御性

内核开发中，**防御性**是一种重要的思维模式。操作系统本身不能出现一点失误，所以必须设置一些机制来实现：  
1. **防止应用程序破坏操作系统**，如果一个应用程序返回恶意值，不处理会破坏操作系统，操作系统应该能够处理它，属于**内核自保**
2. **应用程序不能打破自身的隔离**，恶意程序打破自身隔离后，能够控制内核，从而做任何事情，属于**进程隔离**，程序间不会相互影响  

以上意味着**操作系统**与**应用程序**之间要有**强隔离**，中间的屏障让操作系统可以执行任意策略。通常用**硬件支持**来实现强隔离，有两种，几乎所有的操作系统都支持：  
1. **user/kernel mode**，用户模式/内核模式
2. **virtual memory**，虚拟内存

### User/Kernel mode

本质上是CPU有两种处理模式，一种是用户模式，另一种是内核模式，通过处理器中的一个标志位来确定：  
+ 在运行在**内核模式**时，CPU可执行**特权指令(privileged instructions)**，操作系统的进程就处于内核模式。
+ 而运行在**用户模式**时，CPU只能执行**非特权指令(unprivileged instructions)**

非特权指令就是那些`add`、`sub`等的运算指令、`jr`等的分支指令，任何程序都能够执行，是常见的那些。  

而特权指令是引入**直接操作硬件**的指令，比如配置页表寄存器、设置禁止时钟中断等内核中重要的状态，一旦在用户模式下调用，就会被拒绝，从而控制从用户模式到内核模式，一旦有问题，就直接杀死进程。  

### CPU provides virtual memory

几乎所有CPU都提供了**虚拟内存**。  

总的来说，CPU为每一个进程提供了一个**页表page table**，用于将虚拟地址映射为实际地址。同时，操作系统再为每个进程**分配不相交的物理内存**，通过这种方式使一个进程**只能访问到其页表中对应的物理内存**。  

这样就实现了**进程之间内存的相互隔离**，因为别的进程所在的地址不存在于其页表对应地址中。  

### 控制权转移与进入内核

#### 正常程序系统调用

上述的防御性实现起来过于**严格**，事实上，一个程序可能同时存在用户模式与内核模式权限的指令，譬如ls、find，都需要调用**read**等系统调用，这时就需要把控制权交给内核，靠内核的内核模式来实现。  

在RISC-V中，有指令`ecall <num>`，不同的数字来对应不同的系统调用。其功能是让CPU跳转到内核中的一个特定位置（**系统调用入口**）执行系统调用，并且伴随着严格的检查，以防对操作系统的破坏。  

（这里要想清楚，无论用户态、内核态，都是一段指令的特性，最后干活还得是CPU自己）  

具体的，例如当我们在**shell**中执行自定义的程序中进行**系统调用**`fork()`时，由于程序本身性质是user mode，cpu并不会直接实现这个功能，而是执行`ecall`指令，跳转进入内核中的系统调用入口，这里的状态是内核模式，才实现fork的功能，在实现时还会对参数等进行检查，以`write()`为例，当最后进入内核执行write时，内核会检查写入的地址是否属于程序自身。  

“user mode程序**从未直接拿到钥匙**，只是请求钥匙持有者代劳，并且钥匙持有者执行时会检查是否合法。”  

#### 从恶意程序夺回控制权

上面的程序属于在实现预期功能时，需要用到系统调用，只是自身没有权限，所以主动把控制权交给内核来帮忙；  
但是当遇到恶意程序、或者进入死循环时，user mode的程序一直占用CPU，内核就需要夺回控制权。  

方法是硬件中有一个定时器，在定时器结束后，会强制从当前user mode的程序切换到内核模式的内核调度，然后重新调度CPU给其他进程。  

## 内核：可信任计算基础 Trusted Computing Base

从上面安全性的实现中可以看到，涉及操作系统安全的系统调用，检查参数等关键步骤都由内核来实现，所以内核被称为**可信任计算基础**。  

这意味着：  
+ 其必须是**正确的，没有bug**
+ 其必须将用户程序当成是**恶意的**  

这带来一个问题，究竟什么代码能够运行在内核模式？  
+ monolithic kernel design(宏内核设计)：**将整个操作系统至于内核模式**，是大多unix系统的设定。好处是操作系统中不同的子模块能紧密结合，但全部都放在内核不可避免会带来一些bug 
+ micro kernel design(微内核设计)：**在内核模式中运行尽量少的代码**。好处是小的内核意味更少的bug，但是运行命令时要在内核与外部之间进行多次转换，带来高性能的问题，多见嵌入式操作系统。  

## xv6结构

其**kernel**文件是所有系统调用的统一集成，在Makefile中指定了规定的顺序：  
各个系统调用源码-->经过编译器预处理、编译-->汇编文件.s-->汇编器-->获得目标文件.o，最后通过ld将所有.o文件打包连接为一个kernel文件。  

QEMU与内核约定任何程序的起始地址是0x80000000，以及其可以在编译期间设定内存大小、bios、CPU内核数量、系统镜像加载等。  
因此要将QEMU看作一块真正的实体，尽管其是用c语言模拟的结果。  

具体的，QEMU是一个开源的C语言程序，其本质上是在运行的每一个核心上运行一个死循环，每次读取指令、解码指令、执行指令（包括特权指令与非特权指令）；此外每次循环还要维护寄存器状态。  

***

# Lec4 Page Tables(Virtual Memory)

## Memory Isolation 内存隔离

前面我们知道**隔离**的目的就是，使内核（也就是操作系统）**完全独立**与各种应用程序之外，同时也保证各种应用程序相互独立。  

而**页表**实现的**虚拟内存**就是从内存隔离上来实现隔离。  

### Address Space 地址空间

**基本思想**：给每个程序（当然，包括内核）分配属于自己的地址空间(从0到某个值，相互独立)。  
这样，当一个应用程序失误，访问某个意外值时也不会访问到别的程序的空间。  

**这就是我们实现内存强隔离的目标**，接下来就思考怎么实现**地址空间**。  

### 实现方法：Page Table

归根结底，我们只有一整块DRAM芯片作为物理内存，需要**复用所有不同的地址空间在同一块物理内存上**。  

首先脑中要清晰，有操作系统实现地址空间时，在CPU执行任意带有**地址**的指令时，其访问的都是**虚拟地址**。其将**虚拟地址Virtual Address**交给**内存管理单元MMU**，其根据**页表**将虚拟地址转换为**物理地址Physical Address**，就在这个物理内存地址上执行指令。  
通常页表也保存在内存中，CPU中有专门寄存器存放这个页表地址，在RSIC-V中对应的寄存器是**stap**。  

因此每一个应用程序都有其自己的一套映射（也就是页表），这指明了其地址空间，并且每当CPU切换不同应用程序执行时，stap寄存器中的内容也会随之改变。  

### 具体细节

页表并不是对**每个地址**进行映射（因为逐个地址映射的话，那么仅一张页表的大小就会占用所有内存），而是对**每个页page**（大小为4KB）进行映射。  

#### 从“地址-地址”页表到“页-页”页表

虚拟地址由**两部分**组成，分别是**Index索引**段（27位），与**offset偏移量**段（12位，因为正好能对应一页）：  
+ Index用于在页表中进行映射，从而找到对应的物理内存页编号，
+ Offset索引Index对应到的物理页，然后找到该页中offset对应的条目。

注：尽管RISC-V使用的地址是**64位**的——因为其是64位寄存器，但：  
+ 对于**虚拟地址**，其**实际使用**的只有**39位**，最高的25位是没有使用的，所以虚拟地址空间只有2^39，约512GB；未来可能会有新设计用到这25位
+ 对于**物理地址**，其**实际使用**的是**56位**（硬件设计师决定的），所以物理地址可以比一个虚拟地址大。其中**44位**为物理页号PPN，剩余的**12位**则是页内偏移Offset。

（这里也没理解为什么把**虚拟地址-物理地址**这样的映射换成**虚拟页-物理页**的映射就能解决页表本身内存大小的问题，让GPT举了个例子才理解：内存占用随bit数是呈指数增长的，所以每省1位的对应关系就能让页表体积减小一半，按页存就省下了2^12=4096倍）  

#### 从“页-页”页表到多级页表

事实上，我们发现哪怕用**虚拟页-物理页**的对应，那么一页中也有2^27条，共计约1GB，这样的开销根本经不住。  
实际上，由硬件实现的真正RISC-V的页表结构是**多级结构**。  

前面提到27位的**Index**段，实际上由三个9位的字段构成：  
+ 最开始的9位，是**上层页表目录**（共512个条目）（**stap**寄存器直接存放这个顶级目录的物理地址作为根）的索引，由这9位索引到一个中层页表目录的物理页号（PPN）
+ 中间的9位同理，是**中层页表目录**（也512个条目）的索引，由这9位索引到一个底层页表目录的物理页号（PPN）
+ 最后9位也同理，是**底层页表目录**（也512个条目）的索引，由这9位索引到具体的物理页号  

这样设计的好处是能**有效节省页表空间**，按照前面的**虚拟页-物理页**页表设计，页表中共有2^27个条目；而现在只需要3\*2^9个条目。（其实思路一样，只是发散开，将其串联起来，形成数状结构，节省了空间）  

**注**：我们知道一个**物理地址**是需要**44位页号+12位偏移**的，那么为什么Index段直接通过物理页号PPN就能索引到下层页表的物理地址？  
答案非常巧妙：**一个页表的大小刚好是一页**，因为页表内共有512条页表项，而每个页表项(PTE)的大小是8字节，512\*8B=4KB。  
+ **访问页表方便**，直接通过PPN就能访问到
+ **资源不浪费**，每一级、每一个页表/页都能通过虚拟地址的27位Index访问到
+ **内存管理简单**，页表的操作可以直接复用对页的操作

#### 10位标志位

前面提到，每个页表项PTE占8个字节，也就是64bit，但是其只用了54位，有10位是待未来开发的。  
用到的这**54位**，除了**44位**的物理页号PPN外，剩下的**10bit**是作为标志位的，其很重要。  

+ **valid**位：若设置了valid位，意味着这条**PTE**是包含**有效信息**并可用的。也就是说，最开始的三个页表都只有0号有valid位，其余都没有设置，MMU就不会查询这条PTE。
+ **read**位：表示允许从对应页面读
+ **write**位：表示允许写到对应页面
+ **execute**位：表示可以执行来自对应页的指令
+ **user**位：表示处在user mode的程序也可以访问对应页
+ 其余的不是很重要，用到再说。  

### 开销昂贵——translation look-aside buffer(TLB) 后备缓冲器

从上面的分析中可以看出，在**虚拟地址**到**物理地址**的转换过程中，需要进行**三次内存**访问：中层目录内存地址->底层目录地址->物理地址。  

访存本来就是**开销**大户（记得计组流水线里面讲的？），现在每次访存都要对应三次物理访存，这样的开销应该很大？  

#### TLB

实际上，现代CPU的实现中，在处理器附近有一个缓存，记录最近使用过的地址转换，名字就是**TLB**。  

在每第一次通过遍历三级页表、访问到最终物理地址时，就会把这对应的**\[VA,PA\]**(即virtual address -> physical address)对应映射关系保存在**TLB**中。这样下次访问某虚拟地址时，先在TLB中遍历，而不用遍历三级页表。  
同样的，当发生**进程转换**等导致页表更改，就会触发TLB的刷新清除（对应的指令是`sfence_vma`），确保隔离。  
（当然关于TLB的实现肯定不是直接地址地址对应，会有更多先进高效的方式；但是在本课中不会讨论，因为其具体细节通常对操作系统隐藏，只需要知道有TLB这一东西即可）  

这个TLB的确计组似乎也讲过，并且其底层计算机思想是之前访问过，那么接下来的时间里访问其的概率更大。  

#### 有深度的提问

上面说到的**整个过程**（CPU检查TLB，没有对应的再提供VA给MMU，MMU访问页表得到PA，再访问PA），其实是**简化后的模型**。  

在计组中我们知道，CPU是自己拥有**多级cache**来加速访存的，那么对于真实访存时对于cache中的数据如何访问？  

老师解释到，对于RISC-V的**l1与l2cache**，有些是由**物理地址**索引，有些是**虚拟地址**索引，所以由虚拟地址索引的缓存访问发生在MMU之前，而物理地址索引的缓存是在MMU之后。  

### 页表提供了level of indirection 间接性

这种**间接性**就是从**VA**到**PA**的映射，这个映射完全处在操作系统的控制之下。所以操作系统可以凭借此机制做各种有用的操作。  

## 在xv6中的实现

### 物理地址的详细布局

**物理地址**的布局完全是由**硬件**决定的。  
譬如物理地址从0x80000000开始，才指向DRAM芯片；  
而0x80000000以下的地址则是指向各类io设备。  

而尽管我们使用qemu模拟器，但也是依赖于这特定的规则。  

### 内核虚拟地址的详细布局

当启动xv6后，创建第一个页表、建立第一个虚拟内存空间，其就属于**内核**。  

为了保证xv6的**简洁**，内核空间的VA到PA的映射主要是**恒等映射**，也就是说所有在**PHYSTOP**（物理内存实际最高位地址，因为物理硬件电路虽然支持的地址大于虚拟内存地址，但很少有能真的大于虚拟内存的）之下的虚拟地址与物理地址是相同的。  

而在虚拟内存的更高位置，也分布着别的页面，比如**内核栈kernel stack**就位于虚拟内存的较高位置，其下方有一个**守护页Guard page**，其在物理内存中没有任何映射，对应的PTE也就没有设置有效位，如此就实现了**内核栈溢出->访问守护页->页面错误**。  
**守护页**不会真正消耗物理内存，因为位于虚拟地址的PHYSTOP之上，不手动为其分配映射，就不会占用物理内存。  

**内核栈**有两个映射关系。（以下为个人理解：一个是因其处于大于物理地址最高点的**虚拟内存高位**，但RISC-V虚存39位毕竟小于物理内存的56位，**恒等映射**中必对应有一个**高位物理地址**，只是可能无意义的对应（没有对应硬件）；还有一个映射则是对应在物理内存**PHYSTOP下方**的一个位置，这就是正常、可达的。）  
（另外，**每一个用户进程都对应有一个内核栈kstack**）  

以上这两种就是**页表**的灵活、强大之处（就像指针一样？），同一VA可以映射两个PA，也可以不映射PA，甚至还能多对多。  
xv6中就广泛使用这种技巧，譬如用**守护页**来追踪bug；还有把多个进程的未使用虚拟地址都映射为同一个物理地址，节省空间等（来自提问）  

#### 内核VA->PA中的权限

除了地址到地址的映射之外，还提供了权限的限制、给予。  
譬如内核数据需要读写权限，映射过去就是**RW-**，但是不能执行；  

#### 其他用户模式进程虚拟内存映射

其他user mode应用程序的虚拟内存映射在内核没有使用的物理内存上，用其来保存不同用户程序的文本、数据、页表等。  

理论上其他用户程序的虚拟空间大小是与内核虚拟空间一样大的，但事实上确实占用会少。  

### 实机运行

展示了`kvminit()`等函数的运行，上次讲过，一团乱麻；但现在西湖能稍微理解一些，这就是我对这种**现成的大工程**的恐惧：学习时往往不是从开头就了解，更是先学关键部分，而我总想从一开始就全局掌握整个工程……  

***

# Lec5 RISC-V Calling & Tarp Frames

## C to ASM

无论使用C还是其他语言，机器本身**并不认识**，其只能看懂**汇编语言的二进制编码**，所以当我们说处理器是**RISC-V**还是**x86**，意思都是其可以理解**RISC-V指令集**或**x86指令集**。  

所以每一类处理器都有特有的**指令集 ISA**，每条指令都有一个相关联的二进制编码/操作码，当机器运行时看到操作码就该知道怎么做。  

C->Asm(C编译汇编出来是.S文件)->Binary(obj、o)  

## RISC-V vs x86

两者是两套ISA，通常PC上都是x86（intel与amd）；  
RISC是**精简指令集**，而x86是CISC，即**复杂指令集**。  

x86的复杂在于